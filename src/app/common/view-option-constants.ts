import { KeyValueItem } from "../model/key-value-item";
import { WORD } from "./query-constants";

export const DEFAULT_VIEW_OPTIONS_QUERY_REQUEST_ATTRIBUTES: KeyValueItem[] = [
    new KeyValueItem(WORD, WORD)
  ]